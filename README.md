# CUNY AI Lab Links

This repository contains the static links page. Run `bun run check` before publishing; it checks the committed HTML structure and local asset references without fetching external URLs. The check is the source gate for pull requests and pushes to `main`; Pages deployment remains a separate concern.
